version: "2"
exclude_patterns:
- "**/*.spec.ts"
- "**/*.spec.js"
- "**/*.d.ts"

checks:
  method-complexity:
    config:
      threshold: 10
  return-statements:
    config:
      threshold: 10
  method-lines:
    config:
      threshold: 75

plugins:
  duplication:
    enabled: false
